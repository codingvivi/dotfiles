[Unit]
Description=IMAP IDLE notifier for AOL
After=network-online.target

[Service]
ExecStart=%h/bin/goimapnotify -conf %h/.config/imapnotify/aol.json
Restart=on-failure
RestartSec=30

[Install]
WantedBy=default.target
